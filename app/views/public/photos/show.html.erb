<%= image_tag @photo.get_image(100,100) %><br>

<%# a != b ＝aとbの中身が異なる ==の反対%>
<%# @photoの投稿者とログインしているcustomerが異なっていればいいね出来る %>
<% if @photo.customer != current_customer %>
  <% if @photo.favorited_by?(current_customer) %>
      <p>
        <%= link_to photo_favorites_path(@photo), method: :delete do %>
          ♥<%= @photo.favorites.count %> いいね
        <% end %>
      </p>
      <% else %>
      <p>
        <%= link_to photo_favorites_path(@photo), method: :post do %>
          ♡<%= @photo.favorites.count %> いいね
        <% end %>
      </p>
  <% end %>
<%# 投稿者とログインしているcustomerが同じであればいいねの数だけ表示する %>
<% else %>
  <p>
      ♥<%= @photo.favorites.count %> いいね
  </p>
<% end %>
<h5>撮影者</h5>
<%= image_tag @photo.customer.get_image(100,100) %>
<%= link_to customer_path(@photo.customer.id) do %>
<%= @photo.customer.nickname %>
<% end %>

<% if @photo.customer != current_customer %>
  <% if current_customer.following?(@photo.customer) %>
    <%= link_to "フォロー外す", customer_relationships_path(@photo.customer.id), method: :delete %>
  <% else %>
    <%= link_to "フォローする", customer_relationships_path(@photo.customer.id), method: :post %>
  <% end %>
<% end %>

<br>

<h5>使用カメラ</h5>
  <% @camera_tags.each do |camera_tag| %>
  <%=camera_tag.name%>
  <% end %><br>

<h5>使用加工アプリ</h5>
  <% @editing_app_tags.each do |app_tag| %>
  <%=app_tag.name%>
  <% end %><br>

<h5>撮影者コメント</h5>
<%= @photo.introduction %><br><br>

<% if @photo.customer == current_customer %>
  <%= link_to "編集する", edit_photo_path(@photo) ,class:"btn btn-success" %>
  <%= link_to "削除", photo_path(@photo), method: :delete %>
<% end %>

<div>
  <p>コメント件数：<%= @photo.comments.count %></p>
  <% @photo.comments.each do |comment| %>
    <p><%= image_tag comment.customer.get_image(100,100) %></p>
    <%= comment.customer.nickname %>
    <%= comment.created_at.strftime('%Y/%m/%d') %><%= comment.comment %>
    
    <% if current_customer.reports.find_by(comment_id: comment.id).nil? %>
      <%= link_to "通報する", reports_path(comment_id: comment.id), method: :post %>
    <% else %>
      通報済み
    <% end %>
    
    <% if comment.customer == current_customer %>
      <%= link_to "削除", photo_comment_path(comment.photo, comment), method: :delete %>
    <% end %>
  <% end %>
</div>
<div>
  <%= form_with model: [@photo,@comment] do |f| %>
    <%= f.text_area :comment, rows: '5', placeholder: "コメントをここに" %>
    <%= f.submit "送信する" %>
  <% end %>
</div>
